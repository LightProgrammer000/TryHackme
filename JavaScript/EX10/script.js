// Login

let username = prompt("# Username: ");
let password = prompt("# Password: ");

if (username === "admin" && password === "ComplexPassword")
{
    document.write("Bem vindo Admin !");
}

else
{
    document.write("Login Invalido !");
}